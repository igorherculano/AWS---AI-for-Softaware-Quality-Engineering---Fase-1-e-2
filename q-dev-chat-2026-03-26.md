<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 200px 0 20px 0;">

<div style="font-size: 24px; margin-bottom: 12px;"><strong>Amazon Q</strong></div>
<div style="background: rgba(128, 128, 128, 0.15); border: 1px solid rgba(128, 128, 128, 0.25); border-radius: 8px; padding: 8px; margin: 4px 0; text-align: center;">
<div style="font-size: 14px; margin-bottom: 4px;"><strong>Did you know?</strong></div>
<div>Ask Q to review your code and see results in the code issues panel!</div>
</div>

Select code & ask me to explain, debug or optimize it, or type `/` for quick actions

</div>

---

cls

---

Vou analisar todos os arquivos antes de propor o refactor.

---

Analisei tudo. Aqui está o plano antes de escrever qualquer código:

**Problemas identificados:**
1. Variáveis duplicadas em todos os 4 resources secundários (bloco `*** Variables ***` idêntico ao do principal)
2. `ServerRest_Resources_carrinho.robot` tem `*** Keywords ***` declarado duas vezes
3. Imports de Library (`RequestsLibrary`, `String`, `Collections`) repetidos em todos os arquivos — só o principal precisa deles
4. `ServerRest_Resources_carrinho.robot` tem `Criar Sessão na ServerRest` e keywords do HP (`Criar Usuario Novo Aleatorio`, `Cadastrar Usuario ADM`, `Realizar Login`) chamadas diretamente — já existem no resource principal via import

**Plano de refactor:**
- `ServerRest_Resources.robot` → único dono de variáveis e libraries; sem mudanças nas keywords
- Os 4 resources secundários → removem `*** Variables ***`, removem imports de Library, mantêm apenas `Resource ServerRest_Resources.robot` e suas próprias keywords

Vou aplicar tudo agora:

---

Reescreve o resource principal como único dono de variáveis e libraries, removendo redundâncias internas.

---

Refatora o resource de carrinho: remove variáveis duplicadas, libraries redundantes e o segundo bloco Keywords desnecessário.

---

Refatora o resource de login: remove variáveis duplicadas e libraries redundantes.

---

Refatora o resource de produtos: remove variáveis duplicadas, libraries redundantes e variável local BODY_PRODUTO centralizada no próprio resource.

---

Refatora o resource de usuários: remove variáveis duplicadas e libraries redundantes.

---


Feito. Aqui está um resumo do que foi feito e por quê:

---

## O que foi refatorado

**`ServerRest_Resources.robot` (resource principal)**
- Agora é o único dono das `Library` e de todas as variáveis globais
- Variáveis inicializadas com `${EMPTY}` em vez de valor vazio implícito — prática recomendada no RF
- Removidos logs desnecessários (`Log ${RESPOSTA}` no Listar Usuario)
- Nomes de variáveis locais encurtados onde eram redundantes (ex: `${palavra_aleatorio}` → `${palavra}`)

**Resources secundários (carrinho, login, produtos, usuários)**
- Removidos os blocos `*** Variables ***` com variáveis globais duplicadas — agora herdam tudo via `Resource ServerRest_Resources.robot`
- Removidos os imports de `Library` (RequestsLibrary, String, Collections) — já carregados pelo principal
- `ServerRest_Resources_carrinho.robot`: removido o segundo `*** Keywords ***` duplicado
- `ServerRest_Resources_produtos.robot`: manteve `${BODY_PRODUTO}` e `${ID_PRODUTO_LIMITADO}` em seus respectivos resources pois são variáveis de escopo exclusivo daquele módulo

**Arquitetura resultante:**
```
ServerRest_Resources.robot          ← Libraries + Variáveis globais + Keywords do HP
    ↑ importado por:
    ├── ServerRest_Resources_carrinho.robot   ← ${ID_PRODUTO_LIMITADO}
    ├── ServerRest_Resources_login.robot      ← sem variáveis próprias
    ├── ServerRest_Resources_produtos.robot   ← ${BODY_PRODUTO}
    └── ServerRest_Resources_usuarios.robot   ← sem variáveis próprias
```

Os arquivos de teste (`.robot`) não precisaram de alteração pois seus imports já apontavam para o resource correto de cada módulo.

